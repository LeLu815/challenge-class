1. app 디렉터리 아래에서의 폴더 구조가 그대로 url 경로가 된다.

-> 예외
두 가지의 차이점
1. (폴더명) : url 경로에서 해당 폴더가 제외되지만 라우팅 시스템 내부에는 여전히 포함된다.
2. _폴더명 : url 경로에서 해당 폴더에서 제외되며 라우팅 시스템 내부에서도 제외된다.

-> 라우팅 시스템

app/hello/page.tsx


app/(hello)/page.tsx 
-> app/page.tsx 

app/_hello/page.tsx 
-> app

# use client
언제 쓰나요
1. 컴포넌트 내에서 훅을 사용할 때 => 훅은 대부분 브라우저 환경에서 작동되기 떄문
2. window.kakao.maps => 이런 코드 포함되면 : window, document 같은 브라우저에서만 존재하는 변수를 사용할 때

컴포넌트 앞에 async 를 추가해서 서버컴포넌트를 만들 수 있다.

ssg => Static Site Generation
svg 같은 파일들
안바뀔 값들을 매번 불러오는 것이 아니라 만들어놓고 가져다가 쓰기
빌드할 때 만든다
